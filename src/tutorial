Html Elements

Event

onblur	script	Fires the moment that the element loses focus
onchange	script	Fires the moment when the value of the element is changed
oncontextmenu	script	Script to be run when a context menu is triggered
onfocus	script	Fires the moment when the element gets focus
oninput	script	Script to be run when an element gets user input
oninvalid	script	Script to be run when an element is invalid
onreset	script	Fires when the Reset button in a form is clicked
onsearch	script	Fires when the user writes something in a search field (for <input="search">)
onselect	script	Fires after some text has been selected in an element
onsubmit	script	Fires when a form is submitted



state={
    'email':''
}

this.setState({'email})


Event -> function -> state

You can use state within function

1.Create HTML code


1. Constructor - > Initialise state

2. Functions -> Change state, Write logic state

3. HTML Code  render() -> html


State: save value
Fucntion: change state
HTML : 1. Show state 2. Connect events with function




Email - > onBlur, onchange, onFocus

Phone -> onChange, onBlur, onFocus

Password -> onChange

Password Eye-> onClick

Sign in button -> onClick

Forgot password button -> onClick

Sign in with google -> onClick

Sign in Fb -> onClick


Abhis' system 


Github


My system
